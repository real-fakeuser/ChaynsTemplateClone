/* eslint-disable jsx-a11y/click-events-have-key-events */
import React from 'react';
import PropTypes from 'prop-types';

var ImageProcessorMenu = function ImageProcessorMenu(_ref) {
    var onSave = _ref.onSave,
        onExit = _ref.onExit;
    return React.createElement(
        'div',
        { className: 'cc__image-editor__menu' },
        React.createElement(
            'div',
            {
                onClick: onExit,
                className: 'button button--exit'
            },
            'Abbrechen'
        ),
        React.createElement(
            'div',
            {
                onClick: onSave,
                className: 'button button-save'
            },
            'Speichern'
        )
    );
};

export default ImageProcessorMenu;